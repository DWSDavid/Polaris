# Polaris v2.6 —— 让它真正好用(验证 + 一屏决策 + 变化提醒)

> **执行说明(给 Codex):** v2.5 已完成(218测试绿,大类上卷+AI人话化生效)。本轮三件让系统从"看得懂"到"好用":① 一键研究脚本看真实样本外数字;② 今日决策驾驶舱(一屏给结论);③ 相对昨天的变化提醒。纯逻辑 TDD,脚本/页面按验收点+真网冒烟。每 Task 单独 commit。**复用现有模块,不重造。**

痛点对应:用户要"更容易用"=不用东拼西凑(#2 一屏)+ 不用天天盯(#3 变化)+ 知道这套到底准不准(#1 验证)。

---

## Task 38(#1):一键研究脚本 `scripts/run_research.py` + 报告落盘 + 页面查看
**目标:** 一条命令跑完 v2.4 校准+模型,打印并保存**样本外**报告,且能在 app 里看。
**Files:** Create `scripts/run_research.py`、`src/research/report.py`;Modify `src/app/pages/6_风格风口研究.py` 或新增报告区;Test `tests/test_report.py`
- [ ] **Step1 失败测试(纯函数)** `build_research_report(calib_result, model_result)` → dict 含:`thresholds`(每信号 in_sample/out_sample 胜率)、`model`(oos_auc, oos_beats_baseline, feature_importance 排序)、`generated_at`;`format_report_md(report)` 出可读 markdown。断言关键字段存在、特征重要度按值排序。
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现**
  - `src/research/report.py`:`build_research_report` + `format_report_md` + 落盘 `data/cache/research/latest.json`。
  - `scripts/run_research.py`:拉真实3年板块历史 → `build_dataset` → `train_model`(TimeSeriesSplit)→ `calibrate_threshold`(walk-forward)→ 组装报告 → 打印 markdown + 存 latest.json。命令行可跑 `python scripts/run_research.py`。
  - app 里加"模型体检"区:读 latest.json,展示 **样本外胜率 / OOS AUC / 是否打败基线 / 特征重要度**,并明确标注"样本外不及基线时,以人工判断为主"。
- [ ] **Step4 跑过** + **真网冒烟**(用户机器跑一次,贴样本外数字)。commit `feat(v2.6): 一键研究脚本+样本外报告落盘与查看`

---

## Task 39(#3):EOD 快照 + "相对昨天的变化" `daily_change.py`
**目标:** 每个收盘存一份大类面板快照;对比最近两个交易日,产出"今日变化"。
**Files:** Create `src/pipeline/snapshot.py`、`src/compute/daily_change.py`;Test `tests/test_daily_change.py`
- [ ] **Step1 失败测试**
```python
# tests/test_daily_change.py
import pandas as pd
from src.compute.daily_change import diff_panels

def test_detects_money_turn_and_ignition():
    prev = pd.DataFrame({"group":["原材料","金融"],"main_net_inflow":[-2e8,1e8],
                         "state":["分歧退潮","冷启动"],"ignition_flag":[False,False],
                         "turning_point":[False,False],"rank":[5,3]})
    today = pd.DataFrame({"group":["原材料","金融"],"main_net_inflow":[3e8,1e8],
                          "state":["冷启动","冷启动"],"ignition_flag":[True,False],
                          "turning_point":[False,False],"rank":[2,3]})
    changes = diff_panels(prev, today)
    msgs = " ".join(c["message"] for c in changes)
    assert "原材料" in msgs and ("资金转正" in msgs or "启动" in msgs or "排名" in msgs)
    assert all("group" in c and "kind" in c for c in changes)
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现**
  - `snapshot.py`:`save_snapshot(panel, date)` → `data/cache/snapshots/{date}.parquet`;`load_recent(n=2)`。
  - `daily_change.diff_panels(prev, today)`:逐大类比 —— 资金方向转正/转负、state 变化、新出现启动(ignition False→True)、新拐点、排名上升/下降≥2。每条返回 `{group, kind, message}`,只保留**值得注意**的(过滤无变化)。
- [ ] **Step4 跑过** → PASS。commit `feat(v2.6): EOD快照+相对昨天的变化检测`

---

## Task 40(#2):今日决策驾驶舱(一屏给结论)
**目标:** 打开就一屏看完 —— 今日主线大类 + 我的持仓处境 + 是否对冲 + 该进/该撤 + 加仓后担保比 + 今日变化 + AI 一段结论。**把散在各页的串成一个动作建议。**
**Files:** Create `src/compute/cockpit.py`、`src/app/pages/0_今日决策.py`(设为首屏);Modify `src/ai/advise.py`(吃 cockpit);Test `tests/test_cockpit.py`
- [ ] **Step1 失败测试**
```python
# tests/test_cockpit.py
from src.compute.cockpit import build_cockpit

def test_cockpit_assembles_decision():
    panel = [{"group":"原材料","state":"主升扩散","trend_days":5,"inflow_10d":69e8,
              "ignition_flag":True,"turning_point":False,"position_in_box":1.0,"rank":1}]
    holdings = [{"name":"华泰证券","sector":"证券","group":"金融","shares":159.21,"price":19.7}]
    account = {"total_assets":3143.0,"debt":1860.0,"warning_line":1.5,"liquidation_line":1.3}
    c = build_cockpit(panel, holdings, basket=["原材料"], account=account, changes=[], research=None)
    assert "top_directions" in c and c["top_directions"][0]["group"]=="原材料"
    assert "holding_status" in c and "hedge" in c and "margin" in c and "exit_alerts" in c
    # 高位应给追高提醒
    assert any("高位" in r.get("note","") for r in c["top_directions"])
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现**
  - `cockpit.build_cockpit(panel, holdings, basket, account, changes, research)`:聚合 —— `top_directions`(用 `synthesis.rank_directions`,大类级,带 grounded 理由 + 高位/拐点提醒)、`holding_status`(持仓所在大类的 state/资金/趋势)、`hedge`(用 `divergence`/`portfolio_exposure` 算持仓+候选对冲度)、`exit_alerts`(用 `exit_signal`)、`margin`(用 `risk_engine`:当前担保比 + 加仓情景)、`changes`(传入的今日变化)、`confidence`(若有 research 报告,附该信号样本外胜率)。
  - 页面 `0_今日决策.py`(序号0=首屏):顶部 AI 一段结论(`advise` 吃 cockpit,人话、先结论、限长)→ 下面分卡片:**今日主线大类 / 我的持仓 / 对冲提醒 / 该进该撤 / 担保比 / 今日变化**。套 v2.5 设计语言与名词解释。
- [ ] **Step4 跑过** + 真网冒烟(贴一屏截图)。commit `feat(v2.6): 今日决策驾驶舱(一屏:主线+持仓+对冲+进撤+担保比+变化+AI)`

---

## 完成后报告
- 贴 `python scripts/run_research.py` 的**样本外报告**(阈值胜率、OOS AUC、是否打败基线、Top特征)。
- 今日决策页一屏截图:是否一眼看到"该往哪、持仓如何、是否对冲、该不该动、担保比、今天变了啥"。
- 今日变化检测样例。
- pytest 全绿数。

## 原则
仍不堆新分析功能;这三件都是**把已有能力收口成"好用"**:#1 让你知道准不准、#2 让你一屏决策、#3 让你不用天天盯。做完,Polaris 才算一个你每天愿意打开、看一眼就知道该干嘛的工具。
