# Polaris v2.4 实施计划 —— 智能/regime 层(可解释校准 + 预测 + 风格风口研究)

> **执行说明(给 Codex):** v2.0–v2.3 已完成(180测试绿)。本轮加"从历史学习"的智能层,**全部可解释、抗过拟合**。纯逻辑 TDD,模型/LLM/页面按验收点+真网冒烟。每 Task 单独 commit。**只增强,不推倒已有特征/页面。**

**铁律(否则就是过拟合骗自己):**
1. **一切回测/模型必须 walk-forward 或留样本外**,**同时报告 in-sample 和 out-of-sample** 指标。样本外不优于基线就如实说,不许只报训练集好看。
2. **不做深度网络**(LSTM/Transformer)——样本太小、信噪比低、黑箱、与 grounded/人拍板冲突。只用可解释模型(逻辑回归/梯度提升 + 特征重要度)。
3. 模型只是**辅助校准与佐证**,最终仍**人拍板**,不自动下单。

**数据需求(AKShare/Tushare,⚠️执行时核对函数名):** 3年板块历史(已有 industry_hist)、规模/风格指数(沪深300 vs 中证1000/2000;国证价值 vs 成长)、宏观(10Y国债、北向、两融)。
**新依赖:** `scikit-learn`(加进 requirements.txt)。

参考:复用 `sector_panel_v2`/`cycle`/`ignition`/`divergence`/`synthesis`/`rotation_timeline`/`em_context`/`ai`。

---

# Part A —— 阈值/权重回测校准(替代手调,最高 ROI)

## Task 27:回测评估器 `research/backtest.py`
**目标:** 给定板块历史 + 信号配置,评估信号的历史有效性(命中率/收益/回撤)。
**Files:** Create `src/research/__init__.py`、`src/research/backtest.py`;Test `tests/test_backtest.py`
- [ ] **Step1 失败测试**
```python
# tests/test_backtest.py
import pandas as pd
from src.research.backtest import evaluate_signal

def test_ignition_signal_forward_return():
    # 构造:某周启动=True 后,未来2周确实跑赢
    panel_ts = pd.DataFrame({
        "week":[1,1,2,2,3,3],
        "sector":["A","B","A","B","A","B"],
        "signal":[True,False,False,False,False,False],
        "fwd_return_2w":[0.06,-0.01,0.0,0.0,0.0,0.0],
    })
    res = evaluate_signal(panel_ts, signal_col="signal", return_col="fwd_return_2w")
    assert res["n"] == 1 and res["hit_rate"] == 1.0 and res["avg_return"] > 0.05
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** `evaluate_signal(df, signal_col, return_col)` → `{n, hit_rate, avg_return, median_return, max_drawdown}`;`forward_return(timeline, weeks)` 给每(板块,周)算未来N周相对收益(用 rotation_timeline 产物)。
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.4): 信号回测评估器(命中率/收益/回撤)`

## Task 28:Walk-forward 阈值校准 `research/calibrate.py`
**目标:** 扫启动/离场/方向打分的阈值,**用样本外**挑最优,产出校准值 + in/out-of-sample 胜率。
**Files:** Create `src/research/calibrate.py`;Test `tests/test_calibrate.py`
- [ ] **Step1 失败测试** `calibrate_threshold(df, grid, objective)` 返回 `{best, in_sample, out_sample}`;断言用 train 段挑出的 best 在结构清晰的 mock 上 out_sample 命中率也>基线;`walk_forward_splits(dates, k)` 切时间序列段(不打乱、不泄露未来)。
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** 时间序列 walk-forward 切分;阈值网格扫描按 objective(命中率×收益)在每折 train 选、test 评;聚合输出。**禁止用全样本调参后又在全样本报告。**
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.4): walk-forward 阈值校准(样本外验证)`

---

# Part B —— 可解释预测模型(自动校准方向权重)

## Task 29:特征/标签数据集 `research/dataset.py`
**Files:** Create `src/research/dataset.py`;Test `tests/test_dataset.py`
- [ ] **Step1 失败测试** `build_dataset(timeline, feature_cols, horizon_weeks, win_threshold)` 产出每(板块,周)一行:已算特征(资金/趋势/启动/估值/北向…)+ 标签 `label = 未来horizon周相对收益 > win_threshold`;断言无未来泄露(标签只用 t 之后数据,特征只用 ≤t)。
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** 严格时间对齐组装,丢掉无未来窗口的尾部。
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.4): 训练数据集装配(无未来泄露)`

## Task 30:可解释模型 `research/model.py`(逻辑回归/GBM)
**Files:** Modify `requirements.txt`(+scikit-learn);Create `src/research/model.py`;Test `tests/test_model.py`
- [ ] **Step1 失败测试** `train_model(dataset)` 用**时间序列CV**(TimeSeriesSplit),返回 `{model, feature_importance, in_sample_auc, out_sample_auc}`;`feature_importance` 非空且键=特征名;断言函数不抛错、out_sample_auc 字段存在。`predict_proba(model, row)` 返回 [0,1]。
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** 逻辑回归(标准化)或 GradientBoosting;TimeSeriesSplit 交叉验证;输出系数/特征重要度(可读);持久化模型到 `data/cache/model/`。`derive_direction_weights(feature_importance)` → 把重要度归一成 `synthesis.direction_score` 的权重(自动校准 Task25 的手定权重)。
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 验收** 打印 out-of-sample AUC 与特征重要度;**若 OOS 不优于"全压资金+趋势"基线,在报告里如实标注**。
- [ ] **Step6 commit** `feat(v2.4): 可解释预测模型 + 自动校准方向权重`

---

# Part C —— 风格/风口历史研究 + AI 总结

## Task 31:风格/regime 引擎 `research/regime.py`
**目标:** 近1–3年 规模(大盘↔小盘)与风格(价值↔成长)轮换 + 各期风口 + 宏观背景。
**Files:** Create `src/research/regime.py`;Test `tests/test_regime.py`
- [ ] **Step1 失败测试**
```python
# tests/test_regime.py
import pandas as pd
from src.research.regime import style_spread, detect_epochs, dominant_theme

def test_style_spread_and_epoch():
    big = pd.Series([100,101,102,103,104.0])     # 大盘走强
    small = pd.Series([100,100,99,98,97.0])
    spread = style_spread(big, small)            # >0 = 大盘占优
    assert spread.iloc[-1] > 0
    epochs = detect_epochs(spread)               # 切成风格段
    assert len(epochs) >= 1
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** `style_spread(a,b)`(相对强弱);`detect_epochs(spread)`(滚动符号/趋势切风格段:大盘期/小盘期、价值期/成长期);`dominant_theme(sector_timeline, epoch)`(每段领涨板块=风口);`macro_context(start,end)`(join 10Y/北向/两融,⚠️核对接口)。
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.4): 风格/风口/宏观 regime 引擎`

## Task 32:AI 研究总结 + 页面 `pages/6_风格风口研究.py`
**目标:** 回答"近1/3年趋势、抓什么风口、过往风格转换都因为什么"。
**Files:** Create `src/ai/regime_brief.py`、`src/app/pages/6_风格风口研究.py`;Test `tests/test_regime_brief.py`
- [ ] **Step1 失败测试(mock chat)** `build_regime_prompt(facts)` 含 风格段/风口/宏观字段且只用传入数值,要求分点:近1年总结/近3年总结/各次风格转换及其原因(利率/流动性/政策/盈利)/当前处于什么风格、风口在哪。
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** `regime_brief(facts)` + 页面:上方风格时间线图(大盘↔小盘、价值↔成长 spread)+ 各风格段标注风口 + 宏观叠加;下方 AI 研究总结。grounded、人拍板。
- [ ] **Step4 跑过** → PASS + 真网冒烟
- [ ] **Step5 commit** `feat(v2.4): 风格风口研究页 + AI 历史总结`

---

## 完成后报告
- 校准:启动/离场/方向阈值的 **in-sample vs out-of-sample** 胜率(样本外是否真的有效)。
- 模型:out-of-sample AUC + 特征重要度(哪些信号真重要),是否优于基线。
- 风格页:近3年风格段 + 风口 + AI 对"风格转换原因"的总结样例。
- pytest 全绿数。

## 风险提示(写给执行者也写给用户)
- 若样本外结果**不优于简单基线**,这是**真实结论**,要如实呈现 —— 说明这套信号在历史上没有稳定 alpha,继续以人的判断为主。这比假装模型很准更有价值。
- 校准出的阈值/权重**只是默认建议**,用户可在 config 覆盖;模型概率只是**佐证列**,不替代 grounded 决策。
