# Polaris v2.3 实施计划 —— 视觉美化 + 板块轮动历史(3个月) + 综合"投资方向"

> **执行说明(给 Codex):** v2.0–v2.2 已完成(163测试绿)。本轮三块:① Streamlit 深度视觉美化;② 新增"板块轮动历史(3个月)"可视化页;③ 新增"投资方向"综合页(把主线/资金/启动/北向/龙虎榜/研报/历史类比/对冲串起来)。纯逻辑 TDD,UI/AI 页按验收点 + 截图/真网冒烟。每 Task 单独 commit。**保留现有页面与计算逻辑,只增强不打散。**

**用户决定:** UI 走 **Streamlit 内深度美化**(不换 React)。痛点:现在新闻/context/龙虎榜/主线判断**各管各的,没 connect together**;且缺一个"过往3个月核心板块怎么轮的"视图。

参考:[v2 设计](2026-06-16-polaris-v2-design.md)。复用 `rotation_history.py`、`cycle.py`、`ignition.py`、`divergence.py`、`em_context.py`、`ai/advise.py`。

---

# Part 1 —— 视觉美化(治"太抽")

## Task 21:统一设计系统(主题 + 设计语言)
**Files:** Create `.streamlit/config.toml`;Modify `src/app/ui.py`;Test `tests/test_ui_helpers.py`(纯函数部分)
- [ ] **Step1** `.streamlit/config.toml`:
```toml
[theme]
base = "dark"
primaryColor = "#3DDC97"          # 主升绿
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#161B22"
textColor = "#E6EDF3"
font = "sans serif"
```
- [ ] **Step2** 扩 `ui.py` 设计语言(若已有部分则补齐,保持向后兼容):
  - `STATE_COLORS = {"主升扩散":"#3DDC97","低位修复":"#4C9AFF","冷启动":"#8B949E","龙头孤立":"#E3B341","高位加速":"#F0883E","分歧退潮":"#F85149"}`
  - `state_badge(state)`(带底色 HTML 徽章)、`card(title, body)`(用 `st.container(border=True)`)、`section(title, sub=None)`(统一分区标题)、`plotly_template(fig)`(统一深色模板/字体/无边距/透明底)。
  - 纯函数(state_badge 返回字符串、色值映射)写单测。
- [ ] **Step3** `pytest tests/test_ui_helpers.py -v` → PASS(测 state_badge 含色值、未知状态有兜底色)。
- [ ] **Step4 commit** `feat(v2.3): 统一深色主题 + 设计语言(色板/卡片/分区/Plotly模板)`

## Task 22:把设计语言套到现有页面
**Files:** Modify `Home.py`、`pages/1_行业下钻.py`、`2_风险驾驶舱.py`、`3_决策驾驶舱.py`
- [ ] **Step1** 每页:裸 `st.dataframe` → 统一 `column_config`(进度条/色块/单位);状态列用 `state_badge`;关键块用 `card`;所有 Plotly 过 `plotly_template`。
- [ ] **Step2** 清除任何残留无意义代码块/占位;统一 metric 卡片与留白。
- [ ] **Step3 验收** 逐页 `streamlit run` 截图自检:观感统一、不再"抽"、一屏能读出结论。
- [ ] **Step4 commit** `feat(v2.3): 现有页面套用统一设计语言`

---

# Part 2 —— 板块轮动历史(过往3个月)

## Task 23:3个月轮动时间线装配 `rotation_timeline.py`
**目标:** 拉 ~60–90 交易日 东财行业历史,build 出"每日/每周 × 板块"的强度、净流入、排名时间序列。复用 `rotation_history.py`。
**Files:** Create `src/pipeline/rotation_timeline.py`;Test `tests/test_rotation_timeline.py`
- [ ] **Step1 失败测试**
```python
# tests/test_rotation_timeline.py
import pandas as pd
from src.pipeline.rotation_timeline import build_rotation_timeline, weekly_rank

def test_timeline_shape_and_rank():
    # 两个板块、若干天的 mock 行情(date,sector,pct_chg,amount,main_net_inflow)
    hist = pd.DataFrame({
        "date":["2026-04-01","2026-04-01","2026-04-02","2026-04-02"],
        "sector":["证券","电子","证券","电子"],
        "pct_chg":[2.0,-1.0,1.0,3.0], "amount":[5e10,4e10,5e10,6e10],
        "main_net_inflow":[3e8,-1e8,2e8,4e8],
    })
    tl = build_rotation_timeline(hist)
    assert {"date","sector","strength","rank"} <= set(tl.columns)
    wk = weekly_rank(tl)            # 周度排名(给 bump chart)
    assert {"week","sector","rank"} <= set(wk.columns)
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** `build_rotation_timeline(hist)`:按日算各板块强度(复用 indicators/口径)与排名;`weekly_rank(tl)` 重采样到周给每周板块排名;`leader_changes(...)`(每周/每段领涨板块更替)。数据源:`em_client.industry_hist(sector,start,end)` 对全行业拉 ~90 天,缓存。⚠️ 行业列表来自 `industry_realtime().sector`。
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.3): 3个月板块轮动时间线装配`

## Task 24:轮动历史可视化页 `pages/4_板块轮动历史.py`
**目标:** 一眼看"这3个月核心板块怎么轮的"。
**Files:** Create `src/app/pages/4_板块轮动历史.py`
- [ ] **Step1** 三块可视化:
  1. **排名带状图(bump chart)**:Top N 核心板块的周度排名随时间起伏(谁在哪几周领跑)——Plotly line,y=排名(反向)。
  2. **资金接力热力图**:板块 × 周 的净流入热力(红进绿出),看钱从哪轮到哪。
  3. **龙头更替**:每段领涨板块 + 当时龙头(用 `leader_changes`)。
- [ ] **Step2 Stats 区**:每个核心板块这3个月"强势持续了几段、平均每段多久、当前处于轮动的什么位置(刚启动/主升/退潮)"——用 `summarize_sector_track`。
- [ ] **Step3** 套 Task21 设计语言;`plotly_template`。
- [ ] **Step4 验收** 截图:能讲出"过去3个月主线从 A→B→C 怎么轮的"。
- [ ] **Step5 commit** `feat(v2.3): 板块轮动历史可视化页(排名带状/资金接力/龙头更替+stats)`

---

# Part 3 —— 综合"投资方向"页(把散开的串起来)

## Task 25:方向综合引擎 `synthesis.py`
**目标:** 把已有信号按"板块/方向"聚合成可排序的候选,带 grounded 理由。**这是 connect together 的核心。**
**Files:** Create `src/compute/synthesis.py`;Test `tests/test_synthesis.py`
- [ ] **Step1 失败测试**
```python
# tests/test_synthesis.py
from src.compute.synthesis import direction_score, rank_directions

def test_direction_score_rewards_aligned_signals():
    strong = {"sector":"电力设备","midterm_trend":1,"cum_inflow_20d":40e8,"trend_days":9,
              "ignition_flag":True,"northbound_pos":True,"lhb_active":True,
              "turning_point":False,"valuation_pct":0.4,"hedge_penalty":0.0}
    weak = {"sector":"银行","midterm_trend":0,"cum_inflow_20d":-5e8,"trend_days":1,
            "ignition_flag":False,"northbound_pos":False,"lhb_active":False,
            "turning_point":True,"valuation_pct":0.9,"hedge_penalty":0.3}
    assert direction_score(strong) > direction_score(weak)
    ranked = rank_directions([strong, weak])
    assert ranked[0]["sector"] == "电力设备" and "reasons" in ranked[0]
```
- [ ] **Step2 跑挂** → FAIL
- [ ] **Step3 实现** `direction_score(facts)`:加权 中期趋势 + 多周资金 + 趋势持续 + 启动迹象 + 北向/龙虎榜佐证 + 历史类比胜率,**减** 拐点风险 + 高估值分位 + 对冲惩罚(用 `divergence`)。`rank_directions(list)` 排序并为每个生成 `reasons`(引用真实数值的理由列表)。中长期视角:**偏好"中期趋势在 + 多周资金持续 + 结构完好",对单日脉冲不加分。**
- [ ] **Step4 跑过** → PASS
- [ ] **Step5 commit** `feat(v2.3): 方向综合引擎(多信号加权排序+grounded理由)`

## Task 26:投资方向页 + AI 综合结论 `pages/5_投资方向.py`
**目标:** 回答"从中长期角度,现在比较适合介入的方向是什么",并把新闻/龙虎榜/北向**就地展示**(不再各管各的)。
**Files:** Create `src/app/pages/5_投资方向.py`;Modify `src/ai/advise.py`(接受 synthesis 结果)
- [ ] **Step1** 页面:`rank_directions` 出 Top 3–5 方向卡片,每张含:state/中期趋势/多周资金/启动/估值分位/对冲提示 + **该方向的北向、龙虎榜、研报、近期新闻**(来自 `em_context`,就地内联)。
- [ ] **Step2** 顶部 **AI 综合结论**:把 ranked directions + context 喂 `advise`,输出"中长期较适合的方向是 X(理由…)、其次 Y;回避 Z(对冲/退潮/高估值)。人最后决定。"prompt 仍只用传入真实数值。
- [ ] **Step3** 套设计语言;每个理由可点开名词解释(`glossary`)。
- [ ] **Step4 验收 + 真网冒烟** 截图:一页看到"现在该往哪个方向 + 为什么 + 佐证(北向/龙虎榜/新闻)全在一起"。
- [ ] **Step5 commit** `feat(v2.3): 投资方向综合页 + AI 中长期方向结论`

---

## 完成后报告
- 截图:美化后的首页/轮动历史页/投资方向页。
- 投资方向页能否给出带理由的 Top 方向 + AI 结论,且北向/龙虎榜/新闻就地可见。
- pytest 全绿数。

## 说明
- 阈值微调(回撤带/启动门槛/方向打分权重)仍建议你用真实数据看几天再调,本轮先把可视化与综合跑通。
- 本轮不碰 React 重做;若日后要,再单开。
